[pytest]
testpaths = tests
markers =
    browser: tests that require a browser (Playwright)
    api_only: tests that only use the Google Calendar API (no browser)
    slow: tests that wait for periodic sync (up to 120s)
asyncio_mode = auto
